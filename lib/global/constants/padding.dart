import '../../teethy_exporter.dart';

// * Paddings

/// 32 padding
const EdgeInsets padding32 = EdgeInsets.symmetric(horizontal: 32.0);

/// 16 padding
const EdgeInsets padding16 = EdgeInsets.all(16.0);

/// 8 padding
const EdgeInsets padding8 = EdgeInsets.all(8.0);

// 4 padding
const EdgeInsets padding4 = EdgeInsets.all(4.0);

// 2 padding
const EdgeInsets padding2 = EdgeInsets.all(2.0);
